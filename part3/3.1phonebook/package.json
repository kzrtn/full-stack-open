{
  "name": "3.1phonebook",
  "version": "0.0.1",
  "description": "",
  "license": "ISC",
  "author": "kzrtn",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \\\"Error: no test specified\\\" && exit 1",
    "build:ui": "rm -rf dist && cd ../../part2/2.6phonebook && npm run build && cp -r dist ../../part3/3.1phonebook",
    "deploy:full": "npm run build:ui && git add . & git commit -m uibuild && git push"
  },
  "dependencies": {
    "express": "^5.2.1",
    "mongoose": "^9.7.4",
    "morgan": "^1.11.0"
  }
}
